appId: com.gor3a.dscan
productName: dscan
directories:
  output: release
files:
  - dist/**
  - dist-electron/**
extraResources:
  - from: resources/dscan
    to: dscan
mac:
  target: dmg
  category: public.app-category.utilities
  # unsigned for MVP — no identity / notarization
linux:
  target: [AppImage, deb]
  category: Utility
publish:
  provider: github
  owner: gor3a
  repo: disk-scan
